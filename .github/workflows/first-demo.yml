name: my first flow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout repo
          uses: actions/checkout@v5
          
        - name: Welcome Message
          run: echo "my first peoject"
        
        - name: list files
          run: ls
        